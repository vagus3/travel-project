name: core
description: travel-project 공유 비즈니스 로직(controllers/models/repositories) + 디자인 토큰
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.3
  riverpod_annotation: ^3.0.3
  google_fonts: ^6.3.2
  intl: ^0.20.2
  http: ^1.5.0
  flutter_dotenv: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  riverpod_generator: ^3.0.3
  custom_lint: ^0.8.0
  riverpod_lint: ^3.0.3
  pedantic_mono: ^1.34.0

flutter:
  # 자체 asset 없음 — 각 앱 패키지가 assets/translations, .env 를 직접 소유합니다.
